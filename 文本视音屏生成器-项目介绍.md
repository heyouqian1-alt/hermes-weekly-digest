# 文本视音屏生成器（VoiceCrew）

> 本地多角色对话音频生成器：把“角色名：台词”剧本转换为多角色语音成品，并输出 WAV/MP3 与逐句时间轴。

![产品展示](https://raw.githubusercontent.com/heyouqian1-alt/hermes-weekly-digest/main/%E4%BA%A7%E5%93%81%E5%AE%A3%E4%BC%A0_H5_%E9%95%BF%E5%9B%BE.png)

## 产品简介

文本视音屏生成器是一个围绕本地 Dots-TTS 服务构建的多角色对话工作台。用户只需准备带角色标签的剧本，系统即可自动解析台词、匹配角色音色、逐句调用语音合成服务，再按原始顺序拼接成完整对话音频。它适合播客对话、多人有声内容、课程讲解、角色配音、短视频口播和剧本试听等场景。

本项目定位为外围编排系统，不修改 Dots-TTS 模型本身；语音模型在本机运行，剧本、角色配置和生成结果可留在本地。

## 核心功能

- 多角色音色配置：角色名、参考音频、真实中文转写和角色描述可独立维护。
- 剧本自动解析：优先支持中文冒号“：”，兼容英文冒号“:”。
- 台词继承：没有角色标签的连续行自动归入上一位说话人。
- 动态角色识别：只合成剧本中实际出现且已配置的角色。
- 角色名归一化：可识别从文档复制来的加粗、括号、书名号等外层装饰。
- 串行合成与拼接：逐句调用 Dots-TTS，按剧本顺序生成完整对话。
- 可调轮次停顿：网页滑块或命令行参数控制角色之间的静音时长。
- 实时进度和日志：网页显示任务进度、服务器日志和错误信息。
- 输出完整：WAV、MP3、逐句时间轴 JSON、脚本副本、进度文件和日志。
- 自定义音色：网页支持导入、试听、编辑和管理自定义参考音色。
- 输出目录可配置：可保存自定义绝对输出目录，也可恢复默认目录。
- H5 宣传页：附带产品展示页面和长图资源。

## 工作方式

1. 启动本地 Dots-TTS 服务（默认端口 7860）。
2. 启动本地网站（默认端口 18787）。
3. 在网页中配置角色和参考音色。
4. 输入形如“角色名：台词”的剧本并执行角色检测。
5. 提交合成任务，查看进度和日志。
6. 下载 WAV/MP3 及对应时间轴 JSON。

## 依赖与边界

- Dots-TTS 推理服务：本地 7860 端口。
- Python 运行时：项目交接文档中指定的本地 Python 环境。
- 网站后端采用 Python 标准库 HTTP 服务，尽量保持零额外依赖。
- Dots-TTS 必须保持单实例运行；不要同时启动多个实例。
- 参考音色的真实中文转写是克隆校验所需的必填信息。

## 项目文件

- orchestrator.py：剧本解析、角色调度、逐句合成和音频拼接。
- web/app.py：本地 HTTP 后端及 API。
- web/index.html：角色配置、剧本输入、进度和日志界面。
- launcher.py / 启动网站.bat：网站启动器。
- characters.json：角色与音色配置。
- make_dialogue_video.py：对话音频后续视频制作工具。
- 产品宣传_H5.html：产品宣传 H5 页面。
- 产品宣传_H5_长图.png：产品展示长图。

## 重要说明

本仓库中的宣传页和说明用于介绍项目能力。实际运行时请根据本机环境调整 Dots-TTS 路径、参考音频路径和输出目录；不要把包含个人隐私、授权凭据或大体积生成产物的文件提交到公开仓库。
